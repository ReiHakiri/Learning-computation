This is a folder for a compiler written in Python that converts code written in a small programming language (called CLTM) into a Turing machine transition table.

The folder also contains an implementation of a Turing machine with the turing.py file.
